rem Install PyInstaller from PyPI:
pip install pyinstaller
rem Go to your program’s directory and run:
pyinstaller "Python Radio Stations Tool.py"
rem This will generate the bundle in a subdirectory called dist.